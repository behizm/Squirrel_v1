@model Squirrel.Domain.ViewModels.FileDeleteModel

@using (Ajax.BeginForm("Delete", "Files", null,
    new AjaxOptions { OnFailure = "onDeleteFileError", OnSuccess = "onDeleteFileSuccess" },
    new { id = "delete_file_form" }))
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(m => m.Id)
    @Html.HiddenFor(m => m.FullFilePath)
}