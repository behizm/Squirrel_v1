@model Squirrel.Domain.ViewModels.CommentRemoveModel

@using (Ajax.BeginForm("Remove", "Comments", null, new AjaxOptions { OnSuccess = "onRemoveCommentSuccess" }, new { id = "remove_comment_form" }))
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(m => m.Id)
}

<script type="text/javascript">
    $(function () {
        $.validator.unobtrusive.parse('#remove_comment_form');
    });
</script>