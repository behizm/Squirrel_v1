@model Squirrel.Domain.ViewModels.ConfigRemoveModel

<h4>حذف تنظیم</h4>

@Html.Partial("_Message")

@using (Ajax.BeginForm("Remove", "Configs", null, new AjaxOptions { UpdateTargetId = "option_con_2" }, new { id = "config_details_form" }))
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(m => m.Id)

    <p>
        <button type="submit" class="btn btn-default">حذف</button>
        <button type="button" class="btn btn-default" onclick="$('#option_con_2').html('');">انصراف</button>
    </p>
}

<script type="text/javascript">
    $(function () {
        $.validator.unobtrusive.parse("#config_details_form");
    })
</script>