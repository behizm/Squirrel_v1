@using (Ajax.BeginForm("Search", "Configs", null, new AjaxOptions { UpdateTargetId = "list_result" }, new { id = "config_search_from" }))
{
    <div class="form-group">
        @Html.AntiForgeryToken()
        @Html.Hidden("searchPage", 1)
        @Html.TextBox("keyword", null, new { placeholder = "کلید" })
        <button type="submit" class="btn btn-default">جستجو</button>
    </div>

}