@model Squirrel.Domain.ViewModels.UserRemoveModel

@using (Ajax.BeginForm("Remove", "Users", null,
    new AjaxOptions { OnFailure = "onRemoveUserError", OnSuccess = "onRemoveUserSuccess" },
    new { id = "user_remove_form" }))
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(m => m.Id)
}